{% extends 'base.html.twig' %}


{% block body %}
	<style>
		p {
			border: 1px solid green;
			margin: 10px;
		}

		.jump {
			break-after: always;
		}
		div:not(div:first-child, .container, div:last-child) {
			border: 1px solid black;
			margin: 10px;
		}
	</style>
	<div class="container">

		{% for car in cars %}
			
           {{dump(car)}}
		{% endfor %}

	{% endblock %}
