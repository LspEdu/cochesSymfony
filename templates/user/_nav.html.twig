
<nav class="navbar navbar-expand-lg navbar-light bg-light m-2">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{path('user_index')}}">{{ app.user.userIdentifier }}</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="{{path('user_bill')}}">Facturas</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('car_list')}}">Mis Coches</a>
        </li>
      </ul>
      <form class="d-flex" method="get" action="/user/cars/">
        <input
          class="form-control me-2"
          type="search"
          placeholder="Seat, Mazda..."
          aria-label="Search"
          name="find"
        />
        <button class="btn btn-outline-primary" type="submit">Buscar</button>
      </form>
      <ul class="navbar-nav ms-2">
        <li class="nav-item">
          <a class="btn btn-danger" href="{{path('app_logout')}}">Cerrar Sesión</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
